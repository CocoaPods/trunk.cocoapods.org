#!/usr/bin/env ruby

$:.unshift(File.expand_path('../../', __FILE__))

require 'db/config'
require 'app/models/submission_job'

loop do
  if Pod::PushApp::SubmissionJob.perform_task!
    sleep 0.1
  else
    sleep 1
  end
end
