* Why is the session token length configurable? E.g. token_length

* Add validations to all models.
  - Session: verify token uniqueness.
  - Owner: verify email address.
* Render HTML when confirming a session.
* generate branch names according to https://www.kernel.org/pub/software/scm/git/docs/git-check-ref-format.html
* API error handling
